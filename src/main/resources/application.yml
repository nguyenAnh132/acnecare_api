server:
  port: 8080
  servlet:
    context-path: /acnecare

spring:
  application:
    name: acnecare-api
  datasource:
    url: "jdbc:mysql://localhost:3306/acnecare"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
